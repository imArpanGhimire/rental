{
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"wait-on tcp:3000 && npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "wait-on": "^9.0.1"
  }
}
